AWSTemplateFormatVersion: '2010-09-09'
Description: 'STACK 4 - Elastic Beanstalk Node.js (Learner Lab Compatible)'

Parameters:
  DBPassword:
    Type: String
    NoEcho: true

Resources:

  # ─────────────────────────────────────────────
  # ELASTIC BEANSTALK APPLICATION
  # ─────────────────────────────────────────────
  BeanstalkApp:
    Type: AWS::ElasticBeanstalk::Application
    Properties:
      ApplicationName: myapp-backend
      Description: 'Node.js Express Backend'

  # ─────────────────────────────────────────────
  # ELASTIC BEANSTALK ENVIRONMENT
  # ─────────────────────────────────────────────
  BeanstalkEnvironment:
    Type: AWS::ElasticBeanstalk::Environment
    Properties:
      ApplicationName: !Ref BeanstalkApp
      EnvironmentName: myapp-backend-env
      Description: 'Production Environment Node.js'

      SolutionStackName: '64bit Amazon Linux 2023 v6.10.1 running Node.js 20'

      OptionSettings:

        # ── VPC ─────────────────────────────
        - Namespace: aws:ec2:vpc
          OptionName: VPCId
          Value: !ImportValue myapp-VpcId

        - Namespace: aws:ec2:vpc
          OptionName: Subnets
          Value: !Join
            - ','
            - - !ImportValue myapp-PrivateSubnet1Id
              - !ImportValue myapp-PrivateSubnet2Id

        - Namespace: aws:ec2:vpc
          OptionName: ELBSubnets
          Value: !Join
            - ','
            - - !ImportValue myapp-PublicSubnet1Id
              - !ImportValue myapp-PublicSubnet2Id

        - Namespace: aws:ec2:vpc
          OptionName: ELBScheme
          Value: public

        # ── INSTANCE ───────────────────────
        - Namespace: aws:autoscaling:launchconfiguration
          OptionName: InstanceType
          Value: t3.micro

        # 🔥 WAJIB: pakai LabInstanceProfile
        - Namespace: aws:autoscaling:launchconfiguration
          OptionName: IamInstanceProfile
          Value: LabInstanceProfile

        - Namespace: aws:autoscaling:launchconfiguration
          OptionName: SecurityGroups
          Value: !ImportValue myapp-BeanstalkSGId

        # ── AUTO SCALING ───────────────────
        - Namespace: aws:autoscaling:asg
          OptionName: MinSize
          Value: '1'

        - Namespace: aws:autoscaling:asg
          OptionName: MaxSize
          Value: '3'

        # ── ENV TYPE ───────────────────────
        - Namespace: aws:elasticbeanstalk:environment
          OptionName: EnvironmentType
          Value: LoadBalanced

        # ── LOAD BALANCER ──────────────────
        - Namespace: aws:elasticbeanstalk:environment
          OptionName: LoadBalancerType
          Value: application

        # 🔥 WAJIB: pakai LabRole ARN
        - Namespace: aws:elasticbeanstalk:environment
          OptionName: ServiceRole
          Value: arn:aws:iam::581967380620:role/LabRole

        # ── HEALTH CHECK ───────────────────
        - Namespace: aws:elasticbeanstalk:healthreporting:system
          OptionName: SystemType
          Value: basic

        # ── APP PORT ───────────────────────
        - Namespace: aws:elasticbeanstalk:environment:process:default
          OptionName: Port
          Value: '8080'

        - Namespace: aws:elasticbeanstalk:environment:process:default
          OptionName: HealthCheckPath
          Value: /health

        # ── ENV VARIABLES ──────────────────
        - Namespace: aws:elasticbeanstalk:application:environment
          OptionName: NODE_ENV
          Value: production

        - Namespace: aws:elasticbeanstalk:application:environment
          OptionName: PORT
          Value: '8080'

        - Namespace: aws:elasticbeanstalk:application:environment
          OptionName: DB_HOST
          Value: !ImportValue myapp-DBEndpoint

        - Namespace: aws:elasticbeanstalk:application:environment
          OptionName: DB_PORT
          Value: '5432'

        - Namespace: aws:elasticbeanstalk:application:environment
          OptionName: DB_NAME
          Value: myappdb

        - Namespace: aws:elasticbeanstalk:application:environment
          OptionName: DB_USER
          Value: myappuser

        - Namespace: aws:elasticbeanstalk:application:environment
          OptionName: DB_PASSWORD
          Value: !Ref DBPassword

Outputs:
  BeanstalkURL:
    Description: URL Beanstalk
    Value: !Sub 'http://${BeanstalkEnvironment.EndpointURL}'
    Export:
      Name: myapp-BeanstalkURL
