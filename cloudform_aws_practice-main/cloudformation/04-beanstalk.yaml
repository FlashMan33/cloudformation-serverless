AWSTemplateFormatVersion: '2010-09-09'
Description: 'STACK 4 - Elastic Beanstalk Node.js (Learner Lab Version - FIX MINIMAL)'

Parameters:
  DBPassword:
    Type: String
    NoEcho: true

Resources:

  BeanstalkApp:
    Type: AWS::ElasticBeanstalk::Application
    Properties:
      ApplicationName: myapp-backend
      Description: 'Node.js Express Backend — Dashboard API'
      ResourceLifecycleConfig:
        ServiceRole: arn:aws:iam::581967380620:role/LabRole
        VersionLifecycleConfig:
          MaxCountRule:
            Enabled: true
            MaxCount: 5
            DeleteSourceFromS3: true

  BeanstalkEnvironment:
    Type: AWS::ElasticBeanstalk::Environment
    Properties:
      ApplicationName: !Ref BeanstalkApp
      EnvironmentName: myapp-backend-env
      Description: 'Production Environment — Node.js 20 + ALB + Auto Scaling'
      SolutionStackName: '64bit Amazon Linux 2023 v6.10.1 running Node.js 20'

      OptionSettings:

        # ── VPC ─────────────────────────────
        - Namespace: aws:ec2:vpc
          OptionName: VPCId
          Value: !ImportValue myapp-VpcId

        - Namespace: aws:ec2:vpc
          OptionName: Subnets
          Value: !Join [",", [!ImportValue myapp-PublicSubnet1Id, !ImportValue myapp-PublicSubnet2Id]]

        - Namespace: aws:ec2:vpc
          OptionName: ELBSubnets
          Value: !Join [",", [!ImportValue myapp-PublicSubnet1Id, !ImportValue myapp-PublicSubnet2Id]]

        - Namespace: aws:ec2:vpc
          OptionName: ELBScheme
          Value: public

        # ── INSTANCE ───────────────────────
        - Namespace: aws:autoscaling:launchconfiguration
          OptionName: InstanceType
          Value: t3.micro

        # 🔥 FIX: pakai LabRole (BUKAN string bebas)
        - Namespace: aws:autoscaling:launchconfiguration
          OptionName: IamInstanceProfile
          Value: LabRole

        - Namespace: aws:autoscaling:launchconfiguration
          OptionName: SecurityGroups
          Value: !ImportValue myapp-BeanstalkSGId

        # ── AUTO SCALING ───────────────────
        - Namespace: aws:autoscaling:asg
          OptionName: MinSize
          Value: '1'
        - Namespace: aws:autoscaling:asg
          OptionName: MaxSize
          Value: '4'

        # ── ENV TYPE ───────────────────────
        - Namespace: aws:elasticbeanstalk:environment
          OptionName: EnvironmentType
          Value: LoadBalanced
        - Namespace: aws:elasticbeanstalk:environment
          OptionName: LoadBalancerType
          Value: application

        # ❌ DIHAPUS: ServiceRole (biar AWS handle sendiri)

        # ── HEALTH CHECK ───────────────────
        - Namespace: aws:elasticbeanstalk:healthreporting:system
          OptionName: SystemType
          Value: enhanced

        # ── APP PORT ───────────────────────
        - Namespace: aws:elasticbeanstalk:environment:process:default
          OptionName: Port
          Value: '8080'
        - Namespace: aws:elasticbeanstalk:environment:process:default
          OptionName: HealthCheckPath
          Value: /health

        # ── ENV VARIABLES ──────────────────
        - Namespace: aws:elasticbeanstalk:application:environment
          OptionName: NODE_ENV
          Value: production

        - Namespace: aws:elasticbeanstalk:application:environment
          OptionName: PORT
          Value: '8080'

        - Namespace: aws:elasticbeanstalk:application:environment
          OptionName: DB_HOST
          Value: !ImportValue myapp-DBEndpoint

        - Namespace: aws:elasticbeanstalk:application:environment
          OptionName: DB_PORT
          Value: '5432'

        - Namespace: aws:elasticbeanstalk:application:environment
          OptionName: DB_NAME
          Value: myappdb

        - Namespace: aws:elasticbeanstalk:application:environment
          OptionName: DB_USER
          Value: myappuser

        - Namespace: aws:elasticbeanstalk:application:environment
          OptionName: DB_PASSWORD
          Value: !Ref DBPassword

Outputs:
  BeanstalkURL:
    Value: !Sub 'http://${BeanstalkEnvironment.EndpointURL}'
    Export:
      Name: myapp-BeanstalkURL
